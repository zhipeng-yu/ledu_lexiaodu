# 指定电脑手动测试清单

本清单用于最终顾问演示电脑。自动化测试不能替代真实屏幕缩放、OCR 模型、系统剪贴板和肉眼布局检查。

## 电脑与执行记录

| 项目 | 记录 |
|---|---|
| 电脑资产编号/使用人 |  |
| Windows 版本 |  |
| 屏幕数量、分辨率、缩放比例 |  |
| Python / PaddleOCR 版本 |  |
| 测试知识批次或索引时间 |  |
| 执行人、日期 |  |
| 最终结论 | □ 通过　□ 有条件通过　□ 不通过 |

不得在此表粘贴真实家长聊天正文、截图、手机号或其他个人信息；使用场景编号和虚构演示文本记录问题。

## 执行前检查

| ID | 操作 | 通过标准 | 结果/证据 |
|---|---|---|---|
| E-01 | 确认项目位于独立目录且使用 `.venv` | `where.exe python` 不作为项目解释器依据；所有命令显式使用 `.\.venv\python.exe` |  |
| E-02 | 执行 `.\.venv\python.exe -m pip check` | 显示 `No broken requirements found` |  |
| E-03 | 执行 `.\.venv\python.exe -m lexiaodu --rebuild-knowledge` | 成功输出文档数和切片数，无未审核目录混入 |  |
| E-04 | 执行 `$env:PYTHONIOENCODING='utf-8'; .\.venv\python.exe -B scripts\verify_day5_queries.py` | 四项均为 `[PASS]`，目标资料在 Top 3 |  |
| E-05 | 首次启动后等待 OCR 预加载 | 无模型下载错误；缓存位于配置的 E 盘目录 |  |

## 完整流程

统一使用 `docs/DEMO_SCRIPT.md` 的虚构消息。

| ID | 操作 | 通过标准 | 结果/证据 |
|---|---|---|---|
| F-01 | 启动应用并检查悬浮工具条 | 工具条置顶、按钮可见，在目标缩放下无截断 |  |
| F-02 | 框选单屏聊天区域 | 选区准确；取消与重试可用；跨屏选区明确拒绝 |  |
| F-03 | 等待 OCR | UI 不冻结；识别结果进入校正窗口 |  |
| F-04 | 修改错字、切换发言人、粘贴漏句 | 编辑内容和发言人可修改，空白内容不加入 |  |
| F-05 | 点击“确认无误并生成建议” | 确认前不生成；确认后进入独立“顾问建议”窗口 |  |
| F-06 | 核对检索与建议 | 有顾虑摘要、可编辑回复、事实资料名/定位、风险和转人工状态 |  |
| F-07 | 验证高风险门控 | “投诉”场景未确认时复制禁用，勾选后才启用 |  |
| F-08 | 编辑并复制回复 | 粘贴到空白记事本的是编辑后文本，不是初始文本 |  |
| F-09 | 提交有用/无用反馈 | 只能选择匹配的枚举原因，提交后显示已记录 |  |
| F-10 | 关闭并重启应用 | 聊天和建议不恢复；工具条可正常再次使用 |  |

## 隐私、安全与异常路径

| ID | 操作 | 通过标准 | 结果/证据 |
|---|---|---|---|
| P-01 | 截图前后检查项目目录 | 没有新增 PNG/JPG/BMP 或以聊天内容命名的临时文件 |  |
| P-02 | 用唯一虚构哨兵文本完成一次流程后检查 `artifacts/`、`data/` 和 `*.log` | 不出现哨兵聊天正文或编辑后回复；仅反馈元数据增加 |  |
| P-03 | 检查反馈表结构 | 仅 `id`、`suggestion_id`、`useful`、`reason`、`created_at` |  |
| P-04 | 断开或禁用 OCR 后截图 | 显示可操作错误，能用手动粘贴继续 |  |
| P-05 | 临时移走知识索引后生成 | 明确提示知识索引错误，不编造制度依据 |  |
| P-06 | 使用退款、投诉、安全、隐私、欺凌各一条虚构问题 | 均为高风险且必须转人工 |  |

## 本轮状态说明

Day 5 已在当前开发机完成离屏 UI 自动化、固定查询和代码级隐私验证。上表的实体电脑项目必须由执行人在最终演示电脑上逐项填写；未填写项一律视为“未验证”，不能据此宣称手工验收通过。
