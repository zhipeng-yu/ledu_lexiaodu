# 新任务交接：本地文档与方舟知识库的职责边界

更新时间：2026-08-11

## 本任务要处理的问题

公司 Office 文档已经或即将完整导入方舟知识库。需要单独确认并完成乐小读在此状态下对本地 `company_documents/` 与云端知识库的最终依赖边界。

本交接只记录现状、用户诉求和验收边界，不包含解决方案或技术实现建议。

## 用户明确诉求

- 用户计划把公司文档完整上传到方舟知识库并长期保留。
- 用户询问：云端资料完整后，本地 Office 文档是否还需要保留。
- 用户询问：能否直接在方舟知识库上传本地文件，不经过 TOS 桶。
- 不增加顾问端手动上传按钮。
- 不在本地执行 OCR、正文提取、文本切段或重建知识库。
- 保持现有 PDF 流程不变，除非用户在新任务中明确改变范围。
- 不恢复已经删除的旧功能。

## 当前代码事实

- `src/lexiaodu/advisor_assistant.py` 从本地 `company_documents/` 递归发现 PDF、DOCX、PPTX、XLSX，并把本地相对路径交给 AI 自动选择。
- 当前 Office 文档只有先在本地被发现和选中，才会进入方舟知识库读取流程。
- `src/lexiaodu/office_documents.py` 按本地文件名在方舟知识库文档列表中查找完全同名的文档，再按对应 `doc_id` 检索。
- 当前本地 Office 文件内容不再由应用上传或解析，但本地文件名和路径仍参与发现、路由和同名匹配。
- 当前 PDF 仍从本地读取原始字节，临时上传方舟参与回答，回答后尝试删除方舟临时文件。
- 因此在当前提交中，直接删除本地 Office 文件会使这些文件不再进入 AI 候选列表；删除本地 PDF 会使现有 PDF 流程无法使用。

## 方舟官方能力事实

- 方舟知识库控制台支持本地上传、TOS 导入和公开下载链接等文档导入方式。
- 通过控制台本地上传文档时，不要求用户先把文件放入 TOS 桶。
- 方舟官方文档仅在本地文件数量较多（大于约 100 份）时建议优先使用 TOS 批量导入，并非所有导入都强制依赖 TOS。
- 当前乐小读运行时只读取知识库，不再需要 `TOS_BUCKET` 或 `TOS_ENDPOINT` 配置。

官方依据：

- https://www.volcengine.com/docs/82379/1261883?lang=zh
- https://www.volcengine.com/docs/82379/1528458?lang=zh

## 当前仓库状态

- 项目目录：`E:\Project\ledu_project\lexiaodu`
- 分支：`main`
- 当前已推送提交：`e82f309cb798d3aaa669ac1ff6535dfca5c760e9`
- 提交说明：`feat: read persistent Office documents from Ark`
- 该提交全量测试：52 项通过。
- 用户已明确将此问题拆成独立新任务；本次交接未修改相关功能代码。

## 新任务验收边界

- 明确说明最终哪些格式仍依赖本地文件，哪些格式只依赖方舟知识库。
- 实际程序行为、README、HANDOFF 和环境变量说明保持一致。
- 不误删公司原文档、聊天数据、密钥或现有方舟知识库文档。
- PDF 流程是否变化必须严格服从新任务的明确范围。

