# 乐小读项目交接

更新时间：2026-08-13

## 1. 接手须知

- 项目目录：`E:\Project\ledu_project\lexiaodu`
- 仓库：`https://github.com/zhipeng-yu/ledu_lexiaodu.git`
- 当前分支：`main`
- 开始新任务前完整阅读根目录 `README.md`、本文件和 `AGENTS.md`，并检查 `git status`、当前分支及最近提交。
- 任务完成并验证后，按 `AGENTS.md` 直接提交并推送到 GitHub `main`；不得强制推送。

## 2. 当前有效架构与边界

乐小读是面向公司顾问的独立 AI 会话应用。顾问描述家长问题，AI 决定是否需要公司资料以及选择哪些资料；最终回复由顾问确认后自行发送给家长。

### 知识链路

- 应用只读方舟知识库中解析成功的 PDF、DOCX、PPTX、XLSX 文档。
- 豆包根据当前会话、云端文档 ID 和文件名自动选择最多三份文档；不需要公司资料时可以返回空数组。
- 选中的多个 `doc_id` 必须放在同一次 `search_knowledge` 请求中，不能改回逐文档检索。
- 检索证据连同原文件名交给 Responses API 生成回答。公司事实只能来自明确提供的知识库证据。
- 知识检索链路当前正常。本次后续任务不要重新排查或重构文档发现、文档选择、统一检索、超时策略或云端状态判断。

### 数据与功能边界

- 项目不依赖本地 `company_documents/`、TOS 或方舟 Files API；不要恢复这些依赖。
- 不增加顾问端手动上传，不执行本地 OCR、Office/PDF 正文提取、文本切段或知识库重建。
- 不恢复已删除的截图、附件、回复卡、反馈、旧风险模块或旧知识导入入口。
- 不操作或输出公司原文档、独立备份、云端知识库文档、聊天数据、`.env` 或 `data/chat.key`；删除 `data/chat.key` 会导致既有加密聊天无法读取。
- 启动和环境配置以 `README.md` 为准。

### 关键代码

- `src/lexiaodu/advisor_assistant.py`：文档路由、知识证据消费、角色提示、结构化回答和最终渲染。
- `src/lexiaodu/office_documents.py`：云端文档发现、状态过滤及限定 `doc_id` 的知识检索。
- `src/lexiaodu/app.py`：应用启动及豆包、方舟知识库装配。
- `src/lexiaodu/chat_context.py`、`chat_controller.py`、`chat_repository.py`、`chat_window.py`：上下文、异步回答、加密存储和界面。
- `tests/test_advisor_assistant.py`：本次角色行为的主要聚焦测试。

## 3. 当前已验证状态

- 当前用户被定义为公司顾问，家长是顾问沟通和服务的对象。
- “顾问老师”只负责家长沟通，不是授课老师、班主任或课堂管理人员，不承诺授课、讲题、批改作业、课堂盯课、提醒孩子、管理纪律、观察课堂表现或教学干预。
- 家长话术始终站在顾问身份表达；课堂或教学动作会区分授课老师或实际责任人。反馈、转达或协调其他岗位必须有公司知识或实际流程依据，没有依据时只提示顾问核实。
- 信息不足时向顾问说明缺口并只追问一至两个关键问题。
- 信息充分时固定输出“给顾问的建议”和“可直接发给家长”。
- 不涉及公司事实且上下文充分时，文档路由可以返回空数组，不调用知识检索并直接输出双段建议，也不引用未使用的文档。
- 需要课程政策或产品信息时仍选择并使用知识文档，公司事实只来自检索证据。
- 回答采用严格 JSON Schema，本地渲染器负责固定标题和段落。
- 公司知识来源文件名只保留在顾问说明中，不进入家长话术。
- 名额、订单、付款、App 显示等实时状态要求查询业务系统，不承诺未知结果。
- 聊天案例只影响表达方式，不能补造公司政策、课程信息或承诺。
- 无知识和有知识两条回答路径共用同一 `_SYSTEM_PROMPT`，但分别使用 Chat Completions 和 Responses API。

2026-08-13 自动化验证结果：

- `tests/test_advisor_assistant.py`：19 项通过。
- 全量自动化测试：68 项通过。
- 本次没有进行真实外部会话验证，也没有上传、改写或读取公司原文档与聊天案例。

已知非阻塞边界：上游若未返回合法结构，应用会明确报错，不会把不合约的自由文本直接展示给顾问。

## 4. 未解决事项与下一步

- PDF、Office 文件名、页码和章节引用的真实环境稳定性仍待验收。
- 脱敏并经人工审核的优秀顾问样例学习闭环尚未建设。
- 名额、订单、付款等实时状态尚未连接业务系统。
- 当前没有已授权的下一项代码任务；真实外部会话复核必须先取得用户明确授权。
