# 乐小读项目交接

更新时间：2026-08-13

## 1. 接手须知

- 项目目录：`E:\Project\ledu_project\lexiaodu`
- 仓库：`https://github.com/zhipeng-yu/ledu_lexiaodu.git`
- 当前分支：`main`
- 开始新任务前完整阅读根目录 `README.md`、本文件和 `AGENTS.md`，并检查 `git status`、当前分支及最近提交。
- 当前工作区已有上一任务的未提交修改：
  - `HANDOFF.md`
  - `README.md`
  - `src/lexiaodu/advisor_assistant.py`
  - `tests/test_advisor_assistant.py`
- 上述修改必须保留。任务完成并验证后，按 `AGENTS.md` 直接提交并推送到 GitHub `main`；不得强制推送。

## 2. 当前有效架构与边界

乐小读是面向公司顾问的独立 AI 会话应用。顾问描述家长问题，AI 决定是否需要公司资料以及选择哪些资料；最终回复由顾问确认后自行发送给家长。

### 知识链路

- 应用只读方舟知识库中解析成功的 PDF、DOCX、PPTX、XLSX 文档。
- 豆包根据当前会话、云端文档 ID 和文件名自动选择最多三份文档；不需要公司资料时可以返回空数组。
- 选中的多个 `doc_id` 必须放在同一次 `search_knowledge` 请求中，不能改回逐文档检索。
- 检索证据连同原文件名交给 Responses API 生成回答。公司事实只能来自明确提供的知识库证据。
- 知识检索链路当前正常。本次后续任务不要重新排查或重构文档发现、文档选择、统一检索、超时策略或云端状态判断。

### 数据与功能边界

- 项目不依赖本地 `company_documents/`、TOS 或方舟 Files API；不要恢复这些依赖。
- 不增加顾问端手动上传，不执行本地 OCR、Office/PDF 正文提取、文本切段或知识库重建。
- 不恢复已删除的截图、附件、回复卡、反馈、旧风险模块或旧知识导入入口。
- 不操作或输出公司原文档、独立备份、云端知识库文档、聊天数据、`.env` 或 `data/chat.key`；删除 `data/chat.key` 会导致既有加密聊天无法读取。
- 启动和环境配置以 `README.md` 为准。

### 关键代码

- `src/lexiaodu/advisor_assistant.py`：文档路由、知识证据消费、角色提示、结构化回答和最终渲染。
- `src/lexiaodu/office_documents.py`：云端文档发现、状态过滤及限定 `doc_id` 的知识检索。
- `src/lexiaodu/app.py`：应用启动及豆包、方舟知识库装配。
- `src/lexiaodu/chat_context.py`、`chat_controller.py`、`chat_repository.py`、`chat_window.py`：上下文、异步回答、加密存储和界面。
- `tests/test_advisor_assistant.py`：本次角色行为的主要聚焦测试。

## 3. 已完成的顾问沟通助手改造

2026-08-12 已完成但尚未提交：

- 当前用户被定义为公司顾问，家长是顾问沟通和服务的对象。
- 信息不足时向顾问说明缺口并只追问一至两个关键问题。
- 信息充分时固定输出“给顾问的建议”和“可直接发给家长”。
- 回答采用严格 JSON Schema，本地渲染器负责固定标题和段落。
- 公司知识来源文件名只保留在顾问说明中，不进入家长话术。
- 名额、订单、付款、App 显示等实时状态要求查询业务系统，不承诺未知结果。
- 聊天案例只影响表达方式，不能补造公司政策、课程信息或承诺。
- 无知识和有知识两条回答路径共用同一 `_SYSTEM_PROMPT`，但分别使用 Chat Completions 和 Responses API。

最近一次验证结果：

- `tests/test_advisor_assistant.py`：16 项通过。
- 全量自动化测试：65 项通过。
- 已在明确授权后验证信息不足追问、指定知识资料的双段建议、实时业务状态核实三个真实场景；未保存完整回答、检索证据或公司原文。

已知非阻塞边界：上游若未返回合法结构，应用会明确报错，不会把不合约的自由文本直接展示给顾问。

## 4. 下一任务：纠正顾问与授课老师的角色混淆

### Bug 现象与初步原因

2026-08-13 人工测试输入：“线上上课，孩子自控力差，容易走神、摸手机”。乐小读生成的家长话术错误地以“作为孩子的授课老师”自居，并承诺关注课堂状态、提醒孩子和关注课堂表现。

这是新的岗位边界错误，不是此前“把顾问当家长”的问题。当前 `_SYSTEM_PROMPT` 只说明用户是公司顾问、家长是服务对象，没有明确说明顾问不是授课老师；JSON Schema 和本地渲染器只约束结构，不约束岗位职责。下一任务应先用测试确认这个判断，再做最小修复。

截图只作为人工复现证据，不提交图片，也不把完整回答写入仓库、测试快照或日志。

### 正确角色边界

- “顾问老师”是负责家长沟通的公司顾问，不是孩子的授课老师、班主任或课堂管理人员。
- 顾问可以理解和回应家长顾虑、收集信息、解释有依据的公司信息，并提供后续沟通建议。
- 顾问不能承诺自己授课、讲题、批改作业、设计课堂、课堂盯课、提醒孩子、管理纪律、观察课堂表现或执行教学干预。
- 涉及课堂观察、教学调整、作业讲解等动作时，应写明授课老师或实际责任人。是否可以反馈、转达或协调其他岗位，必须有公司知识或实际流程依据；没有依据时只能建议顾问核实。
- 家长话术必须始终站在顾问身份表达，不能出现“作为孩子的授课老师”“我在课堂上会……”等越权措辞。

### 同时必须保留的无文档能力

学习困难、情绪安抚、亲子沟通和一般学习习惯等不涉及公司事实的问题，不需要强行选择公司文档。现有上下文足够时，应直接给出顾问建议和家长话术。

例如“课后题太难，孩子做不来”的正确路径是：文档路由返回空数组，不调用知识读取器的 `retrieve`，回答仍进入建议模式并输出双段内容，不引用未使用的文档。不能因为没有文档就默认只追问。

### 实施与验收

1. 先在 `tests/test_advisor_assistant.py` 添加复现 Bug 的失败测试，再修改实现。
2. 优先收紧无知识和有知识两条生成路径共用的角色提示，不要只针对截图句子增加单一关键词补丁。
3. 如果考虑程序化拦截或重试，必须先证明高优先级角色提示不足以满足真实场景；不要预先增加复杂后处理。
4. 聚焦测试至少验证：
   - 线上课堂走神场景可以给建议，但顾问说明和家长话术均不把顾问描述为授课老师，也不承诺顾问执行课堂管理或教学动作。
   - “课后题太难”等非公司事实场景可以不选文档、不调用知识检索并直接给双段建议。
   - 需要课程政策或产品信息时仍正常选择和使用知识文档，不编造证据外事实。
   - 涉及其他岗位动作时写明实际责任人，或要求顾问按有依据的流程核实、反馈或协调。
   - 现有信息不足追问、双段输出、来源位置和业务系统核实测试继续通过。
5. 不修改当前知识链路及第 2 节列出的数据与功能边界。
6. 完成后先运行顾问助手聚焦测试，再运行全量测试。真实外部会话验证必须先取得用户明确授权。
7. 更新 `README.md` 和本文件，只保留当前有效状态、实际验证结果和未解决问题。验证通过后直接提交并推送到 GitHub `main`，不得强制推送。

## 5. 可直接复制到新窗口的任务提示词

```text
请继续维护 E:\Project\ledu_project\lexiaodu 项目。

开始前完整阅读 README.md、HANDOFF.md 和 AGENTS.md，检查 git status、当前 main 和最近提交。HANDOFF.md 第 4 节是本次任务的正式交接说明。当前工作区有上一任务的四个未提交修改，必须全部保留。知识检索链路已经正常，不要重新排查或重构它。

唯一任务：修复乐小读把负责家长沟通的公司顾问误认为孩子授课老师的角色边界 Bug。

复现场景：顾问输入“线上上课，孩子自控力差，容易走神、摸手机”，乐小读却在家长话术中以“作为孩子的授课老师”自居，并承诺关注课堂状态、提醒孩子和关注课堂表现。公司顾问即使被称为“顾问老师”，也只负责家长沟通，不负责授课、讲题、批改作业、课堂盯课、提醒孩子、管理纪律、观察课堂表现或执行教学干预。

请先写失败测试复现问题，再做最小修复。无知识和有知识两条生成路径必须遵守同一岗位边界。家长话术始终站在顾问身份表达；涉及课堂或教学动作时，应区分授课老师或实际责任人。反馈、转达或协调其他岗位必须有公司知识或实际流程依据，没有依据时只提示顾问核实，不得擅自承诺。

同时保留无文档建议能力：“课后题太难，孩子做不来”等不涉及公司事实的问题，可以让文档路由返回空数组，不调用知识检索并直接输出“给顾问的建议”和“可直接发给家长”；不要引用未使用的文档，也不要因为没有文档就默认只追问。需要课程政策或产品信息时，仍由 AI 选择相关文档，公司事实只能来自知识库证据。

聚焦测试至少覆盖角色边界、无文档直接建议、公司事实仍使用文档，以及原有信息不足追问、双段输出、来源位置和业务系统核实。不要修改文档发现、文档选择、PDF/Office 统一检索、单次多 doc_id 检索、超时策略或云端状态判断；不要上传或改写公司文档与聊天案例，不执行 OCR、正文提取、文本切段、知识库重建或大批量文件测试，不恢复已删除功能。

完成后运行顾问助手聚焦测试和全量测试。需要真实外部会话验证时先取得我的明确授权。随后更新 README.md 和 HANDOFF.md，只记录当前有效状态和实际结果；验证通过后直接提交并推送到 GitHub main，不得强制推送。
```
