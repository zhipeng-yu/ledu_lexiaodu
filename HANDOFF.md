# 乐小读项目交接

更新时间：2026-08-14

## 当前架构

乐小读是面向公司顾问的 PySide6 独立聊天应用。每个会话独立、在本机加密保存；豆包负责判断是否需要公司资料、向顾问追问或给出回复建议，最终内容由顾问确认后自行发送给家长。

知识链路只读方舟知识库中解析成功的 PDF、DOCX、PPTX 和 XLSX。豆包最多选择三份云端文档，所有选中的 `doc_id` 必须放在同一次 `search_knowledge` 请求中。公司事实只能来自检索证据；实时名额、订单、付款和 App 状态仍需查询业务系统。

关键代码：

- `advisor_assistant.py`：文档路由、知识证据、角色提示、结构化回答和渲染。
- `office_documents.py`：云端文档发现、状态过滤和统一知识检索。
- `chat_repository.py`、`chat_context.py`、`chat_controller.py`、`chat_window.py`：加密会话、上下文、异步请求和界面。
- `app.py`：运行时装配。

## 有效约束

- 当前用户是公司顾问，家长是服务对象；顾问不是授课老师、班主任或课堂管理者。
- 信息不足时向顾问追问；信息充分时输出“给顾问的建议”和“可直接发给家长”。
- 不恢复本地公司文档、TOS、方舟 Files API、本地 OCR、知识切片、旧回复卡、反馈或风险模块。
- 不操作或输出公司原文档、独立备份、云端知识库内容、真实聊天数据、`.env` 或 `data/chat.key`。
- 删除 `data/chat.key` 会导致既有加密会话无法读取。
- 知识检索链路当前正常；截图任务不得顺带重构文档发现、选择、统一检索或超时策略。
- 修改完成并验证后，检查精确 diff，直接提交并推送 GitHub `main`，不得强制推送。

## 当前已验证状态

- 纯文字会话支持新建、搜索、重命名、删除、失败重试和重启恢复。
- 回答使用严格 JSON Schema；无知识和有知识路径共用角色提示。
- 公司知识来源仅出现在顾问说明，不混入可复制家长话术。
- 此前纯文字基线已通过全量自动化测试；截图功能尚未实现或验证，完成后必须重新运行聚焦和全量检查。

## 已确认任务

新增单张聊天截图视觉输入，设计见 `docs/superpowers/specs/2026-08-14-chat-screenshot-vision-design.md`。

确认边界：

- 仅文件选择器，一条消息一张 PNG/JPG/JPEG/WebP；支持长截图，不支持粘贴或多图。
- 原图随会话在本机加密保存，重启可查看和重试，删除会话时一并删除。
- 图片以 Base64 通过 HTTPS 发送到现有 `ARK_MODEL`，使用 `high` 细节模式；不使用 TOS、Files API、知识库图片上传或本地 OCR。
- 个人聊和群聊均自动判断参与者；身份不确定时只追问顾问一个关键问题。
- 首版不做长图切片、视觉转写或派生解析存储；只有真实脱敏验收证明方舟高细节模式不足时再设计切片。

当前状态：设计已由用户确认，等待用户复审书面规格。复审通过后的下一步是使用 `writing-plans` 编写测试驱动的实施计划，尚未授权跳过该门槛直接改代码。

## 仍未解决

- PDF/Office 文件名、页码和章节引用的真实环境稳定性待验收。
- 脱敏且经人工审核的优秀顾问样例学习闭环尚未建设。
- 名额、订单和付款等实时状态尚未连接业务系统。
