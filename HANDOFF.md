# 乐小读项目交接

更新时间：2026-08-17

## 当前状态

乐小读是面向公司顾问的 Python 3.11 + PySide6 Windows 桌面应用。顾问与授课老师的角色边界、无文档直接建议、公司知识回答、单图截图理解和本地加密会话的核心调用均已完成并通过自动化及真实方舟验收；这不代表 README 中尚未完成的长期引用稳定性验证已经完成。

当前源码通过 `python -m lexiaodu` 启动，尚无打包配置、安装器、自动更新或代码签名。下一任务只做面向非技术顾问的 Windows 交付，不再修改已验收的对话行为、截图能力或知识检索链路。

## 必须保留的运行边界

- 应用直接调用豆包和方舟知识库，没有中转服务器。费用归属由云端凭证所属账户决定，与运行电脑无关。
- 知识链路只读方舟中解析成功的 PDF、DOCX、PPTX、XLSX；最多选择三份文档，所有选中 `doc_id` 保持单次 `search_knowledge`。
- 公司事实只能来自检索证据；名额、订单、付款和 App 状态等实时信息仍需查询业务系统。
- 公司原文档不随安装包分发；不恢复本地文档、TOS、方舟 Files API、OCR、本地切片或知识库重建。
- 当前 `.env`、`data/chat.sqlite3`、`data/chat.key`、`data/chat-images/`、`.venv`、测试、缓存和源码不得直接放入安装包。
- `data/chat.key` 受 Windows DPAPI 保护，不能跨电脑或 Windows 用户复制。每台新电脑必须首次启动生成独立密钥和空白数据。
- 密钥、真实配置、聊天内容和构建产物不得提交 GitHub、写入测试快照或输出到日志。

## 下一任务：Windows 一键安装包

### 已确认的交付决策

- 使用者：多位公司顾问老师，多台 Windows 10/11 64 位电脑；每台电脑由一位顾问、一个 Windows 账户使用。
- 形式：一份预配置好的内部 Windows 安装包，安装后无需 Python、命令行、环境变量或手工填写密钥。
- 凭证：所有客户端直接使用用户提供的同一组豆包和方舟知识库凭证，统一由该云账户扣费。用户明确接受凭证落在多台公司电脑并可被提取的风险。
- 分发：安装包只通过公司内部私密渠道发送，不上传 GitHub，也不公开发布。
- 数据：每台电脑从空白聊天开始，在本机独立保存加密数据；不迁移开发机或其他顾问的聊天。
- 安装：优先当前用户安装并尽量不要求管理员权限；创建桌面和开始菜单入口，双击后无控制台窗口。
- 更新：不做自动更新。管理员重新发送新版安装包，顾问双击覆盖安装；升级必须保留该电脑的聊天记录。
- 卸载：卸载程序后仍保留本机聊天数据，避免误卸载丢失记录；彻底清理由管理员手动删除用户数据目录。
- 签名：目前没有代码签名证书，不为本次交付购买；接受首次安装可能出现 Windows SmartScreen“未知发布者”提示。
- 支持：只增加简单中文错误提示、本机日志和“复制诊断信息”，不做设置中心、自助修复、账号系统、中心服务器或费用管理。
- 交付物：安装包和一页极简 `使用说明.pdf`。说明只包含安装、SmartScreen 放行、桌面启动和报错时复制诊断信息；不要为一页说明引入文档生成系统。

### 最小实现要求

1. 先确认项目现有入口和依赖，选择一种自包含的 Windows `onedir/standalone` 构建方式，再用一种简单安装器封装；不要同时维护多套打包工具，也不要优先做启动慢、易误报的单文件自解压方案。
2. 解决当前相对路径依赖：打包资源使用稳定的应用资源路径；聊天数据库、DPAPI 密钥、截图和日志放到当前 Windows 用户可写的 `%LOCALAPPDATA%\Lexiaodu`（名称可按现有项目惯例统一）。不能把可写数据放在 `Program Files`。
3. 构建时只从本机私密配置注入筛选后的模型与知识库必要配置值。安装包可包含这些已获用户授权的运行值，但不能包含完整 `.env` 文件，尤其不能携带未使用的 TOS 配置；不要在源码、构建脚本、命令输出或 Git 历史中写入真实值，也不要增加自制加密、设备绑定或混淆。
4. 首次启动自动创建空数据库、独立 `chat.key`、截图目录和日志目录。覆盖安装和卸载后重装不得覆盖现有用户数据。
5. 缺少配置、断网、API 或知识库调用失败时，显示普通顾问能理解的中文提示，并允许复制不含密钥、家长内容和完整请求的诊断信息。详细日志同样不得记录凭证或真实聊天正文。
6. 安装完成后创建可识别的“乐小读”入口并可立即启动。没有现成代码签名证书，不为图标、自动更新或品牌安装界面扩展任务范围；只做满足使用需要的最小成品化处理。
7. 安装包和最终使用说明输出到 Git 忽略的发布目录；仓库只提交可复现构建所需的非敏感配置、脚本、测试和文档。

### 验收标准

- 在一台未安装 Python、未配置开发环境的干净 Windows 10/11 64 位电脑上，顾问只需双击安装包并按默认步骤完成安装。
- 安装后可从桌面或开始菜单启动，不出现控制台，不要求填写配置；首次进入为空白聊天。
- 至少完成一次真实普通对话、一次知识库回答和一次截图理解，确认统一账户正常扣费且公司资料无需本地分发。
- 在两台电脑上同时完成最小真实调用，确认同一凭证可用、数据互相隔离，并观察是否出现账户并发或限流错误；不为未出现的问题预建限流系统。
- 制造断网或无效配置场景，确认用户看到简短中文提示，可以复制脱敏诊断信息，日志不包含密钥或聊天正文。
- 使用新版安装包覆盖安装后，原聊天和截图仍可读取；在同一 Windows 用户配置仍存在的前提下，卸载后重装也能恢复原本机数据。更换 Windows 账户、删除用户配置或重装系统不属于恢复承诺。
- 安装包内不含开发机现有 `data/`、完整 `.env`、TOS 配置、仓库原始 `.py` 源码树、测试、缓存或 `.venv`；不承诺打包后的程序不可逆向。
- 一页使用说明与实际安装流程一致，并说明未签名安装包的 SmartScreen 放行步骤。
- 完成后运行打包相关聚焦测试和全量测试，记录实际命令与结果；真实付费验收属于本任务已授权范围，但只做上述最小次数。
- 干净 Windows 环境和第二台并发测试电脑或 VM 是最终验收条件；应先利用现有可用环境，确实没有时再向用户请求测试资源，不得用未经实际执行的推断代替验收。

### 非本次范围

- 应用商店、自动更新、中心服务器、用户登录、设备绑定、集中聊天同步、远程停用、用量配额和代码签名采购。
- 重构知识检索、对话提示、截图流程或已有业务功能。
- 解决 README 中现存但与打包无关的业务系统直连、长期引用稳定性或审核样例闭环事项。

## 新窗口启动提示

```text
请继续维护 E:\Project\ledu_project\lexiaodu 项目。完整阅读 README.md、HANDOFF.md 和 AGENTS.md，检查 git status、main 与最近提交。严格执行 HANDOFF.md 的“下一任务：Windows 一键安装包”，已确认的交付决策不要重复询问。使用 $ponytail 选择满足验收标准的最小实现；只有确实影响实现结果且无法从仓库确认的问题再问我。完成验证后精简更新 HANDOFF.md，并按 AGENTS.md 直接提交、推送 GitHub main。
```
